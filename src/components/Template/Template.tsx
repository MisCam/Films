import React from 'react';
import cn from 'clsx';

import s from './Template.module.scss';

type TemplateProps = {

};

const Template = (props: TemplateProps) : React.ReactElement => {
    return (
        <div>
            
        </div>
    );
};

export default Template;